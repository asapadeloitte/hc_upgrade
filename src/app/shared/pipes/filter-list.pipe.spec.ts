
import { Pipe, PipeTransform } from '@angular/core';
import { FilterlistPipe } from './filter-list.pipe';


describe('Pipe: Default', () => {
    let pipe: FilterlistPipe;
    beforeEach(() => {
      pipe = new FilterlistPipe();
    });
    // describe('transform', () => {
    //   it('args length equal to 0 or less than 0', () => {
    //     let value: any;
    //     let args: any;
    //     expect(pipe.transform(value, args)).toBe(value);

    //   });
      // it('args length is greater than 0', () => {
      //   let value: any;
      //   let args:any
      //   expect(pipe.transform(value, args)).toBe(value);

      // });
      });

